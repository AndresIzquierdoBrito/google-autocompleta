# Expo 44's Metro/Webpack resolver expects Expo's peer-style packages to be
# available from the app-level node_modules directory. Hoisted linking also
# keeps the legacy SDK 44 Webpack resolver compatible with pnpm workspaces.
node-linker=hoisted
